{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_주석": "권한 모드와 위험 명령 차단은 ~/.claude/settings.json 에 있다. 여기는 이 프로젝트 것만.",
  "permissions": {
    "allow": [
      "Edit(data/*)",
      "Write(data/*)"
    ]
  }
}
