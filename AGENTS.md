# estate-watch 작업지침

1. 사용자는 개발과 코딩을 처음 접하므로 모든 설명을 쉬운 말로 단계별로 한다.
2. 작업을 시작할 때 `README.md`와 `docs/DAILY_BRIEF.md`를 먼저 읽는다.
3. 매일 아침 브리핑은 `docs/DAILY_BRIEF.md`의 순서를 그대로 따른다.
4. `dist/index.html`은 자동 생성물이다. 직접 수정하지 말고 `web/template.html`이나 `data/*.json`을 고친 뒤 다시 빌드한다.
5. 앱은 항상 **같은 Artifact URL로 재배포**한다. 새 URL을 만들지 않는다.
6. 확인되지 않은 사실을 만들어내지 않는다. 경쟁률·분양가·세대수는 원본에 있는 값만 쓴다.
7. 데이터가 비면 비워 둔 채로 내보내고, 왜 비었는지 사용자에게 말한다.
8. 뉴스 본문은 원문 복사가 아니라 요약으로 쓴다. 원문 링크는 반드시 붙인다.
9. API 키, 비밀번호를 코드에 직접 저장하지 않는다.
